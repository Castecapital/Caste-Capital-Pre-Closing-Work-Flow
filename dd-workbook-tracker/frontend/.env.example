# Only needed if the frontend is deployed on a different origin than the
# backend (e.g. a static host + a separately deployed Node service).
# Leave unset for same-origin deployments (Vite dev proxy, or a reverse
# proxy that serves frontend + backend from one origin) - see DEPLOYMENT.md.
VITE_API_BASE_URL=https://api.example.com
